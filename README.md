# Calci-code
A basic  calculator in C++ typically defines a Calculator class. This class encapsulates data (operands) and methods for operations like addition, subtraction, multiplication, and division. The class structure hides the internal data, exposing only necessary methods to interact with the data. 
